/**
 * Central store barrel – import all stores from here.
 *
 * @example
 * ```typescript
 * import { useAppStore } from '@/src/store';
 * ```
 */
export { useAppStore } from './app-store';
export type { AppState, AppActions, AppStore } from './types';
